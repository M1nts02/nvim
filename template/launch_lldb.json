{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "codelldb",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/Debug/main",
      "args": [],
      "cwd": "${fileDirname}",
      "preLaunchTask": "Build (Debug)"
    }
  ]
}
